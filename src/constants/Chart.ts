/** @format */

import { PixelRatio } from 'react-native'

/** declare static consistent */
export const RADIUS = PixelRatio.roundToNearestPixel(130)
export const STROKE_WIDTH = 24
/** create styled components */
export const CARD_SIZE = RADIUS * 4
export const CONTENT_HEIGHT = CARD_SIZE * 3 + RADIUS
